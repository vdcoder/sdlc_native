langgraph==0.2.45
langchain-core==0.3.15
langchain-community==0.3.5
python-dotenv==1.0.1
requests>=2.31,<3

# --- Optional LLM providers (swap based on environment) ---
# Local inference via Ollama (default, no API key required)
langchain-ollama==0.2.0
# Cloud providers (uncomment / install as needed)
langchain-openai==0.2.9
# langchain-google-genai==2.0.4
# langchain-anthropic==0.3.0
